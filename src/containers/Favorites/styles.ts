import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    flex: 1,
  },
  heading: {
    margin: 20,
    fontSize: 20,
    fontWeight: 'bold'
  },
  emptyContainer: {
    marginTop: 250,
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center'
  },
  empty: {
    fontSize: 16,
    color: '#c2c3c5'
  }
});
